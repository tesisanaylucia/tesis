# Progreso de la tesis

Estado de avance por subsección. Se actualiza como parte del paso 6 del
flujo descripto en `documentacion-tesis/SKILL.md`, cada vez que una tarea
validada amplía una subsección.

Estados posibles: `sin iniciar`, `borrador` (texto conceptual escrito, con
citas y datos volátiles pendientes de verificación), `en progreso`,
`completo (pendiente de revisión)`.

## Capítulo 1: Introducción

| Subsección | Estado | Último prompt | Componente |
|---|---|---|---|
| 1.1 Introducción | sin iniciar | — | — |
| 1.2 Antecedentes | sin iniciar | — | — |
| 1.3 Formulación del Problema y Justificación | sin iniciar | — | — |
| 1.4 Objetivos | sin iniciar | — | — |
| 1.5 Solución propuesta | sin iniciar | — | — |
| 1.6 Metodología | sin iniciar | — | — |
| 1.8 Estructura del documento | sin iniciar | — | — |

## Capítulo 2: Marco Teórico

| Subsección | Estado | Último prompt | Componente |
|---|---|---|---|
| 2.1 Secretarías virtuales / asistentes conversacionales en salud | borrador | Redacción Marco Teórico completo (2026-07-16) | — |
| 2.2 LLM e IA conversacional (function calling) | borrador | Redacción Marco Teórico completo (2026-07-16) | — |
| 2.3 WhatsApp Business Platform (Cloud API) | borrador | Redacción Marco Teórico completo (2026-07-16) | — |
| 2.4 Arquitectura de software (monolito modular, hexagonal, multi-tenancy, reglas como datos, ejecución de tareas programadas) | borrador | Redacción Marco Teórico completo (2026-07-16); ampliada con la extensión de scheduling en FASE-4_PROMPT-6 (TASK-76) | — |
| 2.5 Stack backend (NestJS, Prisma, PostgreSQL) | borrador | Redacción Marco Teórico completo (2026-07-16) | — |
| 2.6 IoT y control de acceso (TTLock) | borrador | Redacción Marco Teórico completo (2026-07-16) | — |
| 2.7 App móvil (React Native) | borrador | Redacción Marco Teórico completo (2026-07-16) | — |
| 2.8 Marco normativo (Ley 25.326 y 26.529) | borrador | Redacción Marco Teórico completo (2026-07-16) | — |

## Capítulo 3: Análisis y Diseño de la Solución

| Subsección | Estado | Último prompt | Componente |
|---|---|---|---|
| 3.1 Caso de estudio | sin iniciar | — | — |
| 3.2 Requerimientos | sin iniciar | — | — |
| 3.3 Selección de tecnologías | sin iniciar | — | — |
| 3.4 Arquitectura general | sin iniciar | — | — |
| 3.5 Modelo de datos | sin iniciar | — | — |
| 3.6 Flujo conversacional | sin iniciar | — | — |
| 3.7 Diseño del prototipo de interfaz móvil | sin iniciar | — | movil |

## Capítulo 4: Desarrollo e Implementación

| Subsección | Estado | Último prompt | Componente |
|---|---|---|---|
| 4.1 Fundaciones | en progreso | FASE-0_PROMPT-8 (TASK-120, corrección: `UsersService` migrado al cliente acotado por tenant — único punto del código que consultaba usuarios sin pasar por la extensión de acotamiento automático, salvo dos lookups de `AuthService.login()` pre-JWT documentados como excepción legítima); FASE-0_PROMPT-7 (TASK-93, corrección a TASK-16/TASK-72: restricción `CHECK` que ata `professionalId` al rol en `User`, con corrección de un efecto colateral en la reconciliación del seed) | backend |
| 4.2 Profesionales | completo (pendiente de revisión) | FASE-1_PROMPT-9 (TASK-92, corrección a TASK-21/22: restricción de unicidad `(professionalId, type, number)` sobre `License`, en reemplazo del índice simple por profesional, con traducción de la violación a un 400 legible en alta y edición) | backend |
| 4.3 Pacientes | completo (pendiente de revisión) | FASE-2_PROMPT-16 (TASK-122, corrección a TASK-29: `PATCH /pacientes/:id/profesionales/:profId` — la edición de la prioridad del vínculo — admitía tanto al profesional del vínculo como a cualquier ADMIN desde su origen, comportamiento documentado y deliberado pero de intención no confirmada contra el SRS, según señaló la misma auditoría de código del 2026-08-14 que originó FASE-3_PROMPT-27/28/29; abierto como tarea de verificación, con la decisión de restringir ya registrada por la usuaria en el ticket antes de iniciar la implementación. Verificado contra la fuente de verdad —el documento de Especificación de Requisitos vigente en Drive, que atribuye la prioridad exclusivamente a "el profesional desde la app", sin vía administrativa— antes de programar. Corregido reemplazando, sólo en ese método, el guard permisivo por `ProfessionalSelfGuard`, la comprobación estricta que TASK-31 ya había introducido para las observaciones del profesional: sin código de autorización nuevo, sólo reutilización. La lectura del vínculo se dejó intacta —un ADMIN sigue pudiendo leer cualquier prioridad—, porque el texto verificado sólo habla de quién la carga y no de quién puede leerla, a diferencia de la frase que sí restringe ambas direcciones para las observaciones); FASE-2_PROMPT-15 (TASK-119, corrección a TASK-32: el lector de planillas decodificaba todo `.csv` como UTF-8 sin resguardo — un `.csv` guardado en Windows-1252/ANSI, el formato exacto que produce "CSV delimitado por comas" en un Excel en español, corrompe en un único byte no válido cada letra acentuada o ñ, y `Buffer.toString('utf8')` reescribe ese byte en silencio como el carácter de reemplazo Unicode en vez de fallar; el nombre corrompido resultante seguía siendo una cadena no vacía que `IsString`/`IsNotEmpty` admitían sin objeción, así que la fila se importaba igual sin ninguna entrada en el informe. Corregido con un chequeo heurístico post-decodificación —conteo de caracteres de reemplazo, el mínimo que la propia auditoría que abrió el ticket proponía frente a una detección de encoding en sentido estricto, descartada por depender de información que la decodificación ya perdió— evaluado sobre cualquier celda de la fila y no sólo las que el importador reconoce, porque una columna ignorada corrompida es evidencia de que toda la fila se decodificó mal aunque nombre/apellido no la delaten. La fila se rechaza y se nombra en el informe en lugar de reintentar la decodificación con otra codificación supuesta, opción descartada por arriesgar un nombre plausible pero incorrecto si el archivo estuviera en una tercera codificación. El mensaje de error es deliberadamente bilingüe, única excepción a la convención de respuestas en inglés del proyecto, a pedido explícito de la usuaria); FASE-2_PROMPT-14 (TASK-106, corrección a TASK-83: la ruta `PATCH /admin/configuracion/patient-inactivity-months` mezclaba idiomas — el segmento `configuracion` en español y el segmento final en inglés — rompiendo la convención de CLAUDE.md de que toda ruta `.../configuracion` es española de punta a punta; corregido renombrando el segmento final a `meses-inactividad-pacientes`, sin período de convivencia con la ruta anterior ya que su único consumidor era la propia suite de pruebas); FASE-2_PROMPT-13 (TASK-98, corrección a TASK-83: el punto de acceso administrativo escribía el umbral y auditaba el cambio en dos pasos sin transacción compartida; corregido dando al método de configuración por inquilino un parámetro de transacción opcional y envolviendo la escritura del umbral y su `audit.log` en un mismo `$transaction`); FASE-2_PROMPT-12 (TASK-83: punto de acceso administrativo para configurar el umbral de inactividad, con tope de doce meses aplicado también en la lectura) | backend |
| 4.4 Motor de Turnos | en progreso | FASE-3_PROMPT-29 (TASK-123, corrección a TASK-78: `GET /admin/feriados` estaba restringido al rol administrador sin excepción, incluida la lectura, y ninguna otra ruta del sistema exponía el calendario de feriados a un profesional — precondición que la futura vista de agenda de la app móvil del profesional (P7.2, TASK-61, todavía no implementada) necesita para marcar los feriados "con color". Corregido con un decorador de roles a nivel de método sobre `findAll`, que `RolesGuard.getAllAndOverride` resuelve antes que el del controlador, dejando las tres escrituras exclusivas del administrador sin cambios; sin cambios de servicio, ya que el listado se resolvía por el cliente de Prisma acotado por inquilino, así que un profesional sólo ve los feriados de su propia organización igual que un administrador. Se descartó incorporar el feriado a la respuesta de disponibilidad o de turnos —la alternativa que el propio ticket ofrecía— por acoplar una preocupación administrativa a dos contratos ya probados con otro propósito, y se descartó un alias de ruta sin el prefijo administrativo por no haber precedente de esa forma en el repositorio); FASE-3_PROMPT-28 (TASK-117, corrección a TASK-40: el ranking de reasignación cargaba `PatientProfessional.type` en cada candidato y nunca lo comparaba —el comparador ordenaba sólo por prioridad explícita, orden de llegada y fecha de creación—, de modo que el criterio de aceptación "los pacientes RECURRENTE tienen prioridad sobre NUEVO" quedaba incumplido en silencio: siendo la prioridad explícita opcional y asignada a mano, dos candidatos sin prioridad se ordenaban puramente por orden de llegada y un recurrente de años no ganaba nada frente a un nuevo anotado antes. Corregido incorporando el tipo como escalón intermedio —entre la prioridad explícita y el orden de llegada, y no como criterio principal, porque la prioridad que el profesional carga a mano sobre un paciente concreto es una decisión más específica que una condición que el sistema deriva solo— y extrayendo el comparador completo, ya de cuatro escalones, a una función pura de nivel de módulo junto a la de peso de prioridad. Punto no evidente: el tipo comparado es el vigente y no el almacenado, porque los vínculos llegan por la carga por lotes de TASK-108, que aplica antes la regla de inactividad de P2.3; sin ese orden la corrección habría dado ventaja en la lista de espera a pacientes que el resto del sistema ya considera nuevos. La ambigüedad de la formulación original de TASK-40 ("si el profesional así lo configuró", condición que no correspondía a ningún parámetro existente) se resolvió con la dueña del producto como regla incondicional, descartando agregar la columna de configuración por tratarse de una opción que nadie modificaría; sin cambios de esquema ni del contrato de la API); FASE-3_PROMPT-27 (TASK-116, corrección a TASK-40: el motor de reasignación de lista de espera era la única ruta de escritura de turnos que no verificaba feriados — por la misma decisión de arquitectura que lo hace escribir directo sobre la tabla de turnos para evitar un ciclo de importación con el módulo de turnos —, de modo que un turno agendado sobre una fecha declarada feriado *después* (el alta de feriado no cancela turnos ya reservados) podía reasignarse automáticamente a un candidato de la lista sobre un día sin atención. Corregido verificando en dos momentos con respuestas distintas: en el paso de ofrecimiento, una vez por recorrido y no por candidato —la condición es propiedad de la franja, idéntica para toda la lista—, sin registrar oferta alguna y liberando la retención; y en el paso de reserva, dentro de la misma transacción que crea el turno, para el feriado declarado mientras una oferta espera respuesta, fallando explícitamente. Punto no evidente: **no** se pudo reutilizar `isSlotFree`, pese a ser lo que sugiere la formulación literal del ticket y lo que aplican las demás rutas — considera ocupada una franja con un turno cancelado bajo retención vigente, y el turno que el motor reasigna es exactamente eso, la fila que él mismo retuvo, de modo que colisionaría consigo mismo de forma determinista e inutilizaría toda la reasignación automática; se extrajo entonces la mitad pertinente a `AvailabilityService.isHoliday`, que `isSlotFree` pasa a consumir, descartando replicar la consulta en el módulo de lista de espera para no crear una tercera definición de la regla. La falla en reserva no reescribe la oferta como rechazada —el paciente sí aceptó, y consignar lo contrario falsearía el registro de auditoría que la Ley 25.326 exige— y la liberación de la retención se acotó con un tipo de error propio, para no confundir el feriado con un cambio de estado concurrente ni enmascarar un fallo de escritura de auditoría); FASE-3_PROMPT-26 (TASK-114, corrección a TASK-35: `Professional.slotCadence` —la cadencia con que se abren turnos en la agenda, separada de la duración de la sesión en TASK-24 justamente para poder expresar el ejemplo del SRS "atención cada una hora con sesiones de 45 minutos"— nunca llegó a la generación de franjas: los dos recorridos de `AvailabilityService` avanzaban siempre con `consultationDuration`, de modo que la configuración se guardaba y no cambiaba nada, y ningún test del repo mencionaba la cadencia. Corregido unificando ambos recorridos en una única grilla parametrizada por un módulo compartido `slot-schedule.ts` (paso = cadencia ?? duración; el corte de bloque sigue exigiendo que entre la sesión completa, y el campo `duration` de la respuesta sigue siendo la duración de la sesión). Alcance obligado más allá del paso: el emparejamiento de la regla de doble franja para paciente nuevo y la colocación de la segunda mitad del turno doble en la reserva pasan a usar la cadencia — con cadencia ≠ duración, lo anterior dejaba de ofrecer turnos de paciente nuevo en silencio y escribía la segunda mitad fuera de la grilla, solapada con un horario que seguía ofreciéndose. Se agregó además la invariante cadencia ≥ duración, verificada sobre el par almacenado dentro de la transacción serializable del endpoint de configuración, porque al hacerse efectiva la cadencia una menor que la sesión abre turnos solapados y ambos reservables); FASE-3_PROMPT-25 (TASK-113, corrección a TASK-80: la retención de 24h de la modalidad MANUAL bloqueaba también la asignación manual del profesional, invirtiendo el criterio de aceptación del propio TASK-80 y la regla del SRS — la ventana restringe la *oferta del bot*, no la mano del profesional, para quien justamente se abre. `Appointment.holdUntil` registraba sólo el vencimiento y no a quién alcanza la retención, de modo que el predicado compartido de "ocupado" trataba igual al bot y al profesional; corregido agregando `Appointment.holdReason` (`MANUAL_REASSIGNMENT` / `AUTOMATIC_OFFER`) con restricción `CHECK` que ata ambas columnas, y el enum `SlotAccess` (`BOT_OFFER` / `DIRECT_ASSIGNMENT`) que `isSlotFree` exige explícitamente: la agenda del bot sigue ocultando ambas retenciones, y la reserva/reprogramación directa queda bloqueada sólo por una oferta automática en curso); FASE-3_PROMPT-24 (TASK-110, mejora sobre TASK-40: `WaitlistService.reorder` esperaba cada escritura de fila una por una dentro de una transacción serializable abierta, en vez de lanzar las N escrituras independientes en paralelo; corregido reemplazando el loop secuencial por `Promise.all`, sin alterar el resultado del reordenamiento ni el manejo de conflictos de serialización); FASE-3_PROMPT-23 (TASK-108, corrección a TASK-40: `rankCandidates` reconsultaba el vínculo paciente-profesional y aplicaba la regla de inactividad una vez por candidato, en vez de usar el batching que `refreshTypes` ya soportaba, costando un número de consultas proporcional a N por cada paso del recorrido de la lista; corregido con `PatientProfessionalsService.findManyByProfessional`, que carga todos los vínculos en una sola consulta y aplica la regla una única vez sobre el conjunto); FASE-3_PROMPT-22 (TASK-104, trazabilidad retroactiva: la migración `20260728171035_harden_integrity_and_drop_redundant_indexes` —tres restricciones `CHECK` sobre duración de turno, rango de fechas de ausencia y orden de lista de espera, y la eliminación de tres índices redundantes de una sola columna— se había fusionado a `main` el 2026-07-28 sin ticket ni entrada de bitácora; documentada retroactivamente sin tocar el cambio en sí, y abierto TASK-112 como seguimiento para el hueco de detección del Stop-hook que lo dejó pasar); FASE-3_PROMPT-21 (TASK-102, corrección a TASK-37: la edad mínima de la modalidad "solo_mayores" estaba fijada como constante de código (18 años) en vez de configuración por organización, a diferencia de la regla hermana de ventana mínima de cancelación que ya seguía ese patrón; corregido agregando la clave `appointment_minimum_age_years` con el mismo mecanismo de fallback y migración de siembra, y reemplazando las dos comparaciones hardcodeadas — reserva y revalidación al reprogramar); FASE-3_PROMPT-20 (TASK-101, corrección a TASK-39/TASK-40: los avisos de reprogramación, de cancelación por ausencia y de oferta de lista de espera armaban su texto a mano en inglés en vez de pasar por NotificationTemplateService (P4.1), por lo que una personalización por tenant de esos textos no tenía efecto; corregido agregando las dos claves de plantilla que faltaban y haciendo que los cuatro puntos rendericen antes de enviar); FASE-3_PROMPT-19 (TASK-100, corrección a TASK-39: la auditoría de reagendado registraba los timestamps anterior/nuevo en `detail` en vez del nombre del campo, violando la regla de CLAUDE.md de que `detail` nombra campos y nunca valores; corregido reemplazando ambos timestamps por `{ fields: ['scheduledAt'] }`, siguiendo el mismo patrón que ya usaban las escrituras de pago/orden); FASE-3_PROMPT-18 (TASK-96, corrección a TASK-40: `DELETE /lista-espera/:id` podía tirar 500 por violación de FK cuando la entrada tenía una `WaitlistOffer` REJECTED/EXPIRED todavía apuntándole, ya que sólo el camino de aceptación de una oferta limpiaba esa referencia; corregido generalizando el mismo mecanismo — anular la referencia dentro de la misma transacción, antes del borrado — al camino de baja explícita); FASE-3_PROMPT-17 (TASK-95, corrección a TASK-39: `rescheduleCore` no reseteaba `confirmationRequestedAt`/`reminderSentAt` al reprogramar un turno, desincronizando las guardas de idempotencia de los crons de auto-cancelación y recordatorio respecto de la fecha nueva; corregido reseteando ambos campos en el mismo punto de escritura); FASE-3_PROMPT-16 (TASK-94, corrección a TASK-40: `GET /lista-espera/profesional/:id` sólo chequeaba tenant, no ownership dentro del tenant; fuga de datos de pacientes entre profesionales de la misma organización, corregida reusando `assertOwnerOrAdmin`) | backend |
| 4.5 Notificaciones y Scheduler | en progreso | FASE-4_PROMPT-2 (TASK-43, P4.2: cron de confirmación a 24h — ventana de detección 23h-25h, columna `confirmationRequestedAt` dedicada a la idempotencia en vez de reutilizar `confirmedAt`, envío vía MessagingPort stub y registro en auditoría); FASE-4_PROMPT-3 (TASK-44, P4.3: cron de autocancelación a las 4h sin respuesta — motivo NO_CONFIRMATION agregado al enum de cancelación, sin extensión del plazo por fin de semana, disparo de ReassignmentPort); FASE-4_PROMPT-4 (TASK-45, P4.4: cron de recordatorio configurable por inquilino — columna `reminderSentAt` dedicada, solo turnos CONFIRMADO — y andamiaje del futuro job de expiración de códigos TTLock, módulo nuevo `access-codes` con un placeholder cada 15 minutos hasta que la entidad AccessCode exista en M6); FASE-4_PROMPT-5 (TASK-82, P4.5: timeout real de 4h para la oferta de turno en lista de espera bajo reasignación AUTOMÁTICA — tabla nueva `WaitlistOffer`, WaitlistResponsePort reemplaza su stub síncrono por un adaptador real, el recorrido de la lista de espera pasa a ofrecer a un candidato por vez y se detiene, `WaitlistOfferTimeoutCron` nuevo cada 15 min vence las ofertas vencidas y avanza el recorrido); FASE-4_PROMPT-6 (TASK-76, P7.b: canal de notificaciones in-app para el profesional — modelo `Notification` sin columna de organización propia, anclado a través del profesional; `GET /notificaciones`, marcar una o todas como leídas, y el listado administrativo por profesional; disparado desde la cancelación de turno y desde la reserva por reasignación; solicitud de receta y error de cerradura modelados sin disparador real todavía; fusionada a `main` el 2026-08-12, commit `3a961b2`); FASE-4_PROMPT-7 (TASK-91, corrección a TASK-82: índice faltante `@@index([organizationId, waitlistEntryId])` en `WaitlistOffer` para respaldar su tercera FK compuesta, sin cambios de lógica); FASE-4_PROMPT-8 (TASK-90, corrección a TASK-76: `Notification.appointmentId`/`.prescriptionRequestId` pasan de FK simple a FK compuesta contra `professionalId` — la columna que su propio padre determina, ya que `Notification` no lleva `organizationId` propio —, con `ON DELETE CASCADE` y los dos índices de soporte faltantes); FASE-4_PROMPT-9 (TASK-97, corrección a TASK-82: `respondToOffer` cambiaba el estado de una `WaitlistOffer` fuera de transacción y auditaba varias líneas después sin `tx`; corregido envolviendo el `updateMany` guardado y su `audit.log` en un mismo `$transaction`, siguiendo el patrón ya existente en `cancelAndReassign`); FASE-4_PROMPT-10 (TASK-99, corrección a TASK-43/TASK-45: los crons de confirmación y de recordatorio enviaban el mensaje de WhatsApp antes de aplicar su guard de idempotencia, sin re-verificar el `status` del turno — solo el `findMany` inicial del batch lo filtraba, así que un turno que cambiaba de estado entre esa lectura y su procesamiento individual igual recibía un mensaje contradictorio; corregido invirtiendo el orden en ambos — el `updateMany` guardado, ahora también filtrado por `status`, corre primero junto con su `audit.log` en la misma transacción, y el mensaje solo se envía si esa escritura afectó una fila); FASE-4_PROMPT-11 (TASK-109, mejora de reuso/simplificación sobre TASK-43/TASK-44/TASK-45/TASK-82/TASK-89: los cinco crons de esta sección repetían de forma idéntica el patrón "recorrer organizaciones, resolver el usuario SYSTEM de cada una, abrir su contexto de inquilino, omitir con advertencia la que no tuviera usuario SYSTEM", cada uno con su propia copia del comentario sobre la particularidad de `AsyncLocalStorage`; extraído a un helper compartido `runForEachOrganizationAsSystem` (`src/common/scheduling/`), función simple con las dependencias como parámetros —mismo estilo que `runSerializable`—, sin cambios de comportamiento observable); FASE-4_PROMPT-12 (TASK-111, verificación sin cambios de código: la duplicación del helper `pad` entre los crons de confirmación y de recordatorio que reportaba una auditoría del 2026-08-12 ya no existía — TASK-101 la había consolidado antes, como efecto colateral de mover otros call sites al motor de plantillas; mismo caso que TASK-81/TASK-78, auditoría corrida sobre una copia desactualizada del repositorio; ticket cerrado como Listo con comentario documentando la verificación, sin PR asociado); FASE-4_PROMPT-13 (TASK-118, corrección a TASK-43: el mensaje de confirmación a 24h no informaba la duración de la sesión que pide el SRS — la plantilla base no declaraba marcador de duración y el cron no seleccionaba `Appointment.duration`. Corregido con el marcador `{durationMinutes}` en el texto base y el dato en el `select`, decidiendo que el marcador transporte sólo el número y la palabra "minutos" quede en la plantilla —misma separación que `{scheduledAt}`/`{time}`, para que la unidad siga siendo personalizable por inquilino—, y leyendo la duración de la columna del turno y no de la configuración vigente del profesional. Punto no evidente, fuera de lo que pedía el ticket: cambiar la constante no alcanzaba, porque `20260812140000_seed_notification_templates` ya había escrito el texto viejo en `OrganizationConfig` de toda organización y `resolveTemplate` prefiere la fila del inquilino, de modo que el arreglo no se habría visto en ninguna base ya migrada; se agregó una migración de datos que actualiza sólo la fila que conserva íntegro el texto base anterior. El criterio de aceptación se probó sobre el texto entregado a `MessagingPort` con el `NotificationTemplateService` real, no sobre los parámetros de un doble: un desacuerdo de nombres entre cron y plantilla lanza `MissingNotificationTemplateParamsError`, que el catch "mejor esfuerzo" del cron degrada a una línea de log) | backend |
| 4.6 Capa conversacional y WhatsApp | completo (pendiente de revisión) | FASE-5_PROMPT-1 (TASK-46, P5.1: adaptador del AIPort con GPT-4o mini vía el SDK `openai` — primer adaptador real del puerto, hasta ahora un stub desde Fundaciones; proveedor elegido según el documento de evaluación de tecnologías de IA conversacional del Objetivo Específico 2 (costo y madurez de function calling), no Claude Sonnet como el título del ticket sugiere — una primera versión se implementó por error sobre Anthropic y se rehizo sobre OpenAI en la misma sesión; contrato ampliado a historial multi-turno, tools y prompt de sistema opcional con tipos de dominio propios, no los del SDK, que no necesitaron cambios al cambiar de proveedor; reintento propio en el adaptador —429/5xx, backoff exponencial, tope de 3 intentos— con el reintento incorporado del SDK apagado para que sea observable y testeable; API key nunca llega al adaptador, sólo al provider que construye el cliente); FASE-5_PROMPT-2 (TASK-47, P5.2: catálogo de once herramientas del chatbot — disponibilidad/reserva/confirmación/reprogramación/cancelación/consulta de turnos, búsqueda o alta de paciente, registro y verificación de consentimiento, solicitud de receta, FAQ — cada una un adaptador delgado sobre un servicio de dominio ya existente, ensambladas por una única función `defineTool` (validación class-validator + resolución del actor SYSTEM + conversión de excepciones HTTP de Nest en resultados descriptivos, nunca una excepción al modelo); agrega el modelo `Faq` (similitud por superposición de palabras, sin embeddings/pg_trgm) y `PrescriptionRequestsService` (sólo crea con estado pendiente, sin disparar la notificación ya declarada pero sin llamador en el esquema); resuelve el actor SYSTEM desde el contexto de inquilino ya abierto, sin parámetro de organización, que es la respuesta concreta al requisito de marca blanca del ticket; la herramienta de alta de paciente termina exigiendo más campos que la firma ilustrativa del ticket, reutilizando el DTO completo del alta HTTP para no crear pacientes no reservables; rama creada sobre la de TASK-46, no sobre `main`, por la dependencia real de tipos); FASE-5_PROMPT-3 (TASK-48, P5.3: orquestador de conversación — `OrquestadorService.procesar(sessionId, mensajeEntrante, organizationId)` abre el contexto de inquilino una vez por turno (igual que un interceptor HTTP por petición, cumpliendo lo que el comentario de `resolveSystemActor` de TASK-47 ya dejaba anotado como pendiente), arma el system prompt del inquilino, ejecuta el ciclo de llamado a herramientas hasta texto plano o hasta un tope de diez vueltas, y lanza `ToolCallLimitExceededError` sin persistir el turno atascado si lo agota; historial de sesión en memoria (`ConversationSessionStore`) con expiración perezosa en la lectura, mismo criterio "sin cron" que la regla de inactividad de pacientes; umbral de inactividad (30 min por defecto) y texto base del system prompt configurables por inquilino en `OrganizationConfig`, sembrados en una migración de datos además del seed; `{organizationName}` interpolado sobre el texto con el mismo mecanismo `{placeholder}` de las plantillas de notificación, cuyas dos funciones puras se trasladaron a una ubicación compartida (`src/common/templates/`); se extrajo `ConfigTenantService.getPositiveInteger`, generalizando un patrón de lectura de umbral que ya repetían cuatro lugares del código; `AIPort.processMessage` recibe una copia del historial en cada vuelta del ciclo, no el arreglo mutable en construcción; `buildSessionId(organizationId, mobilePhone)` queda listo para el futuro webhook de WhatsApp (TASK-53); rama creada sobre `main`, corregido a mitad de sesión un error de proceso donde el trabajo había arrancado directamente sobre `main` sin ramificar); FASE-5_PROMPT-4 (TASK-49, P5.4: capa de guardrails determinísticos — `GuardrailService.checkIncomingMessage` corta el turno antes de llamar a `AIPort` o a cualquier herramienta si el mensaje del paciente trae una palabra clave de urgencia médica o de crisis de salud mental; `GuardrailService.checkResponse` revisa la respuesta final en texto plano contra cuatro reglas independientes —observaciones del profesional, monto de copago, derivación a un operador humano, datos de un paciente ajeno a la conversación (DNI suelto o referencia nombrada en tercera persona)— y reemplaza la respuesta completa por uno de tres textos canónicos compartidos entre reglas afines; el texto reemplazado, no el original, es el que queda guardado en el historial de la sesión; reglas globales y no configurables por inquilino, a diferencia del resto de la configuración del chatbot, por pedido explícito del ticket; `stripDiacritics` extraída a `src/common/text/normalize-text.ts`, compartida con `FaqService`; sin cambios de esquema ni de migración); FASE-5_PROMPT-5 (TASK-50, P5.5: los cinco flujos conversacionales principales —reservar, confirmar, reprogramar, cancelar y consultar turnos— más la identificación del paciente por documento y la verificación de consentimiento previa a la reserva. En una arquitectura de function calling el flujo lo conduce el modelo, de modo que la tarea aportó tres cosas y no una máquina de estados por flujo: el manual de procedimiento `CONVERSATION_FLOWS_PROMPT`, anexado al system prompt de cada turno y deliberadamente fuera de la mitad que el inquilino puede sobrescribir —una clínica que personalizara el tono borraría, si no, el procedimiento de reserva completo—; las herramientas que faltaban para que ese procedimiento fuera ejecutable; y la aplicación determinística del único paso que el SRS atribuye al sistema y no al bot. Herramienta nueva `list_professionals`, sin parámetros y sin especialidad —el SRS establece que el paciente elige por nombre con independencia de ella, y un filtro que la herramienta no puede expresar es más fuerte que una instrucción en el prompt—, delegando en el listado activo que ya servía `GET /profesionales` y por lo tanto acotada al inquilino por construcción. `check_consent` pasa a devolver, en el mismo resultado y sólo en la rama que lo necesita, el texto de la plantilla SOLICITUD_CONSENTIMIENTO ya renderizado, para que la redacción de un consentimiento de la Ley 25.326 no quede librada a la improvisación del modelo. `list_patient_appointments` pasa de delegar en el listado general a un método nuevo, `listActiveForPatient`, que devuelve sólo los turnos activos y futuros con el nombre del profesional, con el conjunto de estados activos derivado de la máquina de estados de TASK-38 en lugar de reenumerado. El ofrecimiento de reprogramación posterior a una cancelación se aplica de forma determinística sobre el texto final del turno —mismo punto y misma razón que los guardrails—, disparado por las herramientas que tuvieron éxito y no por el texto, de modo que una cancelación rechazada por la ventana de cuatro horas no lo genera; esa ventana no se replicó en la capa conversacional, se dejó donde ya estaba. Punto no evidente, fuera de lo que pedía el ticket: el system prompt pasa a informar la fecha del día, sin la cual el modelo no puede traducir "la semana que viene" al rango que la consulta de disponibilidad recibe. El celular dejó de pedirse por completo, por precisión de la usuaria durante la sesión: se guarda del contacto de WhatsApp que pide el turno, dato que la clave de sesión de TASK-48 ya transporta, así que el parámetro se retiró del esquema de la herramienta de alta y se lee de un `ConversationContextService` nuevo —equivalente por turno de `TenantContextService`, abierto por el orquestador dentro de él— más `mobilePhoneFromSessionId`; se escribe sólo en el alta y nunca sobre una ficha existente, para no reescribir en silencio el dato de contacto de la clínica. Discrepancia registrada entre el ticket y el SRS —el ticket pide nombre, apellido y celular para crear un paciente; el SRS y el alta implementada exigen documento, fecha de nacimiento y contacto de emergencia— resuelta a favor del SRS, porque un paciente creado con menos datos no podría reservar. Nueve pruebas de conversación extremo a extremo contra la base real con el AIPort simulado, que verifican cada flujo por el estado que deja en la base; sin cambios de esquema ni de migración); FASE-5_PROMPT-6 (TASK-51, P5.6: las tres reglas de negocio conversacionales — validación de edad del paciente nuevo, información de obra social y preguntas frecuentes — sin cambios de esquema, porque las cuatro columnas necesarias (solo_mayores y tipo_atencion en Profesional, fecha_nacimiento en Paciente, primera_sesion en el vínculo), la tabla FAQ y la aceptación de obras sociales ya existían de fases anteriores. La regla de admisión de paciente nuevo, hasta ahora escrita dos veces dentro del servicio de turnos con redacciones distintas, se extrajo a un módulo puro que ahora leen sus tres consumidores: la reserva, la revalidación de la reprogramación y la nueva herramienta `check_booking_eligibility`, que permite finalizar el flujo antes de ofrecer horarios; el motivo del rechazo viaja como discriminante y no como texto, separando por primera vez "no hay fecha de nacimiento registrada" de "es menor de edad" — caso que, corregido a pedido de la usuaria y con respaldo en el SRS ("el chatbot validará la edad solicitando el DNI y la fecha de nacimiento"), no deriva a la clínica sino que pide el dato: `find_or_create_patient` ganó un tercer camino que completa los datos obligatorios de reserva ausentes, escribiendo sólo lo que falta y nunca pisando un valor ya cargado, vía el `PatientsService.update` auditado, y la lista de datos obligatorios —escrita hasta ahora tres veces— se extrajo a `booking-data.rule.ts`; la verificación previa es salida temprana y nunca control, con una prueba que reserva ignorándola y verifica que la reserva rechaza igual. El predicado de "paciente nuevo" es primera_sesion y no tipo, porque la regla de inactividad degrada el tipo tras un año sin concurrir y la reserva ya usaba primera_sesion. La consulta de obra social se resolvió ampliando `list_professionals` con tipo de atención y obras sociales por nombre, sin herramienta nueva, porque esa lectura ya las cargaba y el flujo de reserva la invoca de todos modos; el tipo de atención se informa con dos valores y no con la tercera opción combinada que pedía el ticket, que ni el DER ni el esquema respaldan. Los importes quedaron fuera de alcance por decisión del ticket, alineado con el guardrail monetario. El texto de "no tengo esa información" viaja dentro del resultado de `answer_faq`, igual que el texto de consentimiento, para que el modelo no improvise una respuesta sobre una clínica que no conoce. Se corrigió de paso una filtración detectada al validar: la herramienta de identificación devolvía la entidad de Prisma —con organizationId y la fecha de nacimiento como instante— en lugar de la respuesta presentada. Seis pruebas de conversación extremo a extremo, una por criterio de aceptación, más tres del completado de la fecha de nacimiento ausente —incluida la verificación de la traza de auditoría— y pruebas unitarias de los dos módulos puros y de la lectura de elegibilidad; sin cambios de esquema ni de migración); FASE-5_PROMPT-7 (TASK-52, P5.7: solicitud de recetas por el chatbot y manejo de mensajes fuera de alcance, sin cambios de esquema. El flujo de receta no necesitó herramienta nueva: la lista de profesionales tratantes del paciente ya viaja en el resultado de `find_or_create_patient` desde la corrección de presentador de TASK-51, de modo que el modelo puede detenerse si viene vacía y preguntar por cuál si trae más de una; debajo, el servicio afirma el vínculo de tratamiento antes de escribir —lo que la clave foránea compuesta hacia `PatientProfessional` exige igual—, con una prueba que registra ignorando la instrucción y verifica que nada se escribe. `request_prescription` pasa a devolver forma presentada (id, estado y texto de confirmación) en lugar de la entidad, y el texto de confirmación —que reúne el "días hábiles" del ticket y el recordatorio del horario habilitado que pide el SRS— viaja con el resultado, igual que el texto de consentimiento y el de FAQ. Se conectó la notificación `PRESCRIPTION_REQUESTED`, declarada en el esquema desde TASK-76 sin llamador y nombrando a esta tarea como su emisor, con el mismo patrón de mejor esfuerzo fuera de la transacción que usan cancelación y reasignación. En el manejo fuera de alcance se separaron los dos textos que TASK-49 había unificado —urgencia y fuera de alcance— y se agregó una regla nueva de guardrail, `no_clinical_advice`, evaluada sobre la RESPUESTA del bot y no sobre el mensaje del paciente: un síntoma mencionado camino a pedir un turno es un pedido legítimo, mientras que no hay caso en que corresponda que el bot enuncie un diagnóstico o una dosis; las expresiones son acotadas para no chocar con el propio flujo de recetas. El manual de flujos importa ambos textos del módulo de guardrails en lugar de copiarlos, de modo que lo que se le pide al modelo y lo que el sistema envía son la misma cadena. Seis pruebas de conversación extremo a extremo más pruebas unitarias de servicio, herramienta y guardrails); FASE-5_PROMPT-8 (TASK-53, P5.8: adaptador real de `MessagingPort` contra la WhatsApp Business Cloud API de Meta, reemplazando al *stub* de Fundaciones, más `WhatsappModule` con el webhook HTTP que recibe los mensajes entrantes y es el primer punto de entrada real de `OrquestadorService.procesar`, hasta ahora sólo invocable desde pruebas. Credenciales de envío globales y no por inquilino, mismo criterio que `OpenAiAdapter`, porque la firma de `sendMessage` ya fijada por TASK-46 no lleva identificador de número de origen. Modelo nuevo `WhatsappPhoneNumber` (acotado por organización) resuelve a qué inquilino pertenece un número entrante; se lee sin acotar por inquilino, mismo caso que `UsersService.findByEmail`, porque es único en toda la base y es la propia fila la que determina qué contexto abrir. La firma de cada mensaje se valida con HMAC-SHA256 contra el cuerpo crudo del pedido —no contra su reserialización, que podría diferir byte a byte— habilitando `rawBody` a nivel de toda la aplicación, con `timingSafeEqual` para no filtrar el resumen por tiempo de respuesta; un número sin mapeo y una notificación de estado de entrega se descartan sin invocar al orquestador, ambos con 200. Conectar un adaptador que puede fallar de verdad expuso que ninguno de los cinco llamadores previos de `sendMessage` atrapaba una falla de envío; se corrigió sólo la prueba de conversación de reprogramación que pasó a fallar por eso (simulando el puerto, mismo mecanismo que ya usaban otras pruebas), dejando registrado como pendiente fuera de alcance el resto de los llamadores sin protección. Se corrigió además, al validar contra una base nueva, un defecto real y anterior en la migración de siembra del *system prompt* de TASK-48 (`to_jsonb` sin *cast* a `text`, que PostgreSQL rechazaba desde que se escribió)); FASE-5_PROMPT-9 (TASK-54, P5.9: suite de conversación de punta a punta contra el `POST /webhook` real firmado y verificada contra el espía de `MessagingPort`, para los nueve flujos del ticket, cerrando el Módulo 5; puerto de IA con dos modos elegidos por una variable de entorno —simulado por defecto, real detrás de `USE_REAL_AI=true` dejando conectado `OpenAiAdapter`— sin cambiar el código de las pruebas entre uno y otro, con sólo las aserciones que dependen de la redacción o secuencia exacta de un modelo condicionadas al modo; grupo de pruebas aparte que reemplaza el cliente de `openai` y el `fetch` de `WhatsAppCloudAdapter` por dobles que lanzan si se invocan, para comprobar contra el límite real de red que el modo simulado no llama a ninguna API externa; corregido un defecto anterior que impedía arrancar la aplicación en integración continua por variables de entorno faltantes en `ci.yml`; tres piezas de las pruebas existentes del módulo extraídas a un directorio de soporte compartido, sin cambio de comportamiento)); FASE-5_PROMPT-10 (TASK-77, P5.b: ABM administrativo `GET/POST/PATCH/DELETE /admin/faqs` sobre la tabla `Faq` que el Módulo 5 dejaba sin vía de gestión desde TASK-47, cerrando la deuda que el propio comentario de `faq.module.ts` anotaba; sin cambios de esquema. Patrón copiado del ABM de feriados (TASK-78) — rol ADMIN a nivel de clase, aislamiento entre inquilinos por "no encontrado" en vez de "prohibido" — con `ParseUUIDPipe` en el parámetro de ruta en lugar del `ParseCalendarDatePipe` a medida de feriados, porque `Faq` tiene `id` propio y no una clave natural compuesta con fecha. Contrato JSON en `question`/`answer`, los nombres que el modelo ya usaba desde TASK-47, no `pregunta`/`respuesta` como sugiere literalmente el ticket; `DELETE` responde `204` sin cuerpo, ya que borrar una FAQ no tiene efecto colateral que reportar y ninguna fila del chatbot referencia su identificador; las tres escrituras quedan auditadas dentro de su propia transacción)) | backend |
| 4.7 Cerradura TTLock | completo (pendiente de revisión) | FASE-6_PROMPT-5 (TASK-59, P6.5, cierra el Módulo 6: tolerancia a fallos de la cadena nube → TTLock → Gateway → cerradura. `TTLockAdapter.createTemporaryCode`/`.verifyCodeInstalled` ganan una política de reintentos con backoff exponencial —máximo 3 intentos totales, misma lectura que TASK-46 dio a la frase idéntica del ticket del adaptador de IA— sobre 429/5xx/timeout, este último posible por primera vez gracias a un timeout por pedido que el adaptador no tenía desde TASK-55; `LockOperationError` gana un campo `transient` fijado en el punto de lanzamiento. El registro de cada intento no podía vivir en el propio adaptador —sólo `AccessCodeService` conoce el turno—, así que `LockPort` gana un callback opcional `onAttempt` que el adaptador invoca sin conocer su propósito, y que el servicio usa para escribir una fila `CODE_ATTEMPT` en `LockLog` por intento, el evento que el propio comentario de `LockLog.attemptNumber` ya anticipaba desde TASK-55. La falla definitiva, además de la fila `ACCESS_CODE_ERROR` de `LockLog` que ya existía desde TASK-56 (ahora con el número de intento real), pasa a escribir también un asiento en `REGISTRO_AUDITORIA` —el nombre que el ticket usa en mayúsculas para la tabla real, distinto de la mención genérica en minúscula que reserva para el registro de intentos—, deliberadamente no atado a la misma transacción que la anulación del código, por tratarse de información diagnóstica de mejor esfuerzo y no del registro de "qué cambió". La notificación al profesional sigue sin usar `MessagingPort`, como pide el ticket textualmente: ni `Professional` ni `User` tienen número de celular en este esquema, la misma limitación que ya había encontrado TASK-58. `generateForAppointment`/`revokeForAppointment` ganan `actorId`, encadenado desde `AppointmentsService` a través de sus tres puntos de enganche. Sin cambios de esquema/migración); FASE-6_PROMPT-1 (TASK-55, P6.1: primera implementación real de `LockPort` (declarado desde TASK-18) — `TTLockAdapter` contra la TTLock Open Platform API, con autenticación OAuth 2.0 de credenciales de cliente cacheada y renovada por organización, PIN de 6 dígitos generado con `node:crypto`, y un error descriptivo con el código HTTP de la respuesta para las dos formas en que TTLock informa una falla (rechazo HTTP y sobre `{errcode, errmsg}` propio sobre 200). Credenciales por inquilino, no globales, leídas de `ConfigTenantService` bajo una única clave `ttlock_config`. Se creó además el modelo `AccessCode`, que la descripción del ticket daba por preparado desde TASK-34 pero que, según el propio comentario de esa tarea en el esquema, sólo dejó lista la posibilidad de la FK contra `Appointment`, no la tabla — se resolvió a favor de la evidencia documentada de forma consistente en el propio repositorio, que asignaba su creación a esta tarea. Con `AccessCode` en el esquema se cerró el "estado transicional" que `LockLog` arrastraba desde TASK-34, cableando sus dos referencias como claves foráneas reales (`turnoId` renombrado a `appointmentId`) con eliminación en cascada. Fuera de alcance, respetado: integración con el flujo de turnos — TASK-56; barrido real de vencimiento — TASK-57; tolerancia a fallos con reintentos — TASK-59); FASE-6_PROMPT-2 (TASK-56, P6.2: `AccessCodeService.generateForAppointment`, enganchado al único punto donde un turno pasa a CONFIRMADO (`AppointmentsService.confirm`) — calcula la ventana de validez (turno − 15 min, turno + duración + 1 h sobre la duración grabada del propio turno), crea el passcode vía `LockPort.createTemporaryCode`, lo verifica vía `LockPort.verifyCodeInstalled` y sólo entonces persiste `CODIGO_ACCESO` activo. Una verificación fallida, o una falla de `LockPort` antes de llegar a ella, no persiste ningún código activo ni afecta la confirmación del turno — dispara en cambio un evento `ACCESS_CODE_ERROR` en `LockLog` (literal y campo de intento ya anticipados por TASK-55) y una notificación `LOCK_ERROR` al profesional, como lo único que puede significar hoy "el flujo de tolerancia a fallos de TASK-59". Idempotencia por lectura indexada, sin transacción serializable, apoyada en que la propia máquina de estados del turno ya impide una segunda confirmación bajo el flujo normal. Fuera de alcance, respetado: envío del código al paciente — TASK-58; barrido real de vencimiento — TASK-57; reintentos y tolerancia a fallos — TASK-59); FASE-6_PROMPT-3 (TASK-57, P6.3: cierre del ciclo de vida de `CODIGO_ACCESO` — `AccessCodeService.revokeForAppointment` (elimina el passcode de la cerradura como mejor esfuerzo y anula la fila igual si esa llamada falla, devolviendo si había o no un código activo) enganchado en `AppointmentsService.cancel`, `cancelForAbsence` y `rescheduleCore`; en la reprogramación, el booleano de retorno decide si corresponde emitir un código nuevo para la fecha nueva, evitando generarlo sobre un turno todavía no confirmado. Se completó `AccessCodeExpirationCron` (placeholder desde TASK-45) como barrido independiente por fecha de vencimiento, no como reutilización de la revocación —el propio ticket lo exige como red de seguridad aun si los enganches anteriores fallan—, marcando expirado y auditando `ACCESS_CODE_EXPIRED` (traducción al inglés del "EXPIRACION_CODIGO" del ticket, mismo criterio que `WAITLIST_OFFER_EXPIRED`) cada código vencido. Se extrajo `resolveLockId` a una función compartida en cuanto el cron se volvió su segundo llamador, y se generalizó el registro de fallas de `LockPort` para distinguir generación de revocación y referenciar el `CODIGO_ACCESO` concreto en esta última. Bug real corregido en validación: el cron comparaba contra `new Date()` en vez de `new Date(Date.now())`, rompiendo el control de reloj por `jest.spyOn` que ya usan los demás crons del proyecto); FASE-6_PROMPT-4 (TASK-58, P6.4: los dos procesos que le faltaban al módulo según el SRS — envío del código al paciente una vez activo, y apertura ad-hoc por un profesional o administrador ante una falla. El envío se agregó al mismo método privado de `AppointmentsService.confirm` que ya disparaba la generación desde TASK-56 (renombrado `sendAccessCode`), renderizando la plantilla nueva `ACCESS_CODE_DELIVERY` del motor de P4.1 y reutilizando el `notifyPatient` que ya usaban cancelación y reprogramación; el criterio "no enviar si no quedó activo" no exigió verificación propia, porque `generateForAppointment` ya devuelve `null` en cualquier desenlace que no termine en código activo. La apertura ad-hoc agregó `AccessCodeService.generateAdhoc`, `POST /cerradura/abrir-adhoc` (rol profesional o administrador del inquilino) y `AccessCodesController` nuevo: genera un código de quince minutos sin turno, lo persiste con `id_turno` nulo, audita `ACCESS_CODE_ADHOC_OPENING` dentro de la misma transacción y devuelve el PIN en la respuesta HTTP — no por `MessagingPort`, porque no existe número de celular registrado para `Professional`/`User` en este sistema, y el propio ticket ofrece esa alternativa. A diferencia de `generateForAppointment`/`revokeForAppointment`, este método no traga una falla de `LockPort`: no hay otra mutación que proteger, y la traduce a un 503 para que el solicitante se entere en el momento. El requisito "id_turno=null en CODIGO_ACCESO" obligó a rediseñar el acotamiento por inquilino de `AccessCode`, hijo puro de `Appointment` desde TASK-55: ganó `organizationId` propio (acotamiento directo) y una clave foránea compuesta *opcional* hacia `Appointment` —mismo mecanismo de `WaitlistOffer.waitlistEntryId`, sin restricción mientras la columna sea nula—, en vez de dejar sin acotamiento automático a una tabla que gobierna el acceso físico al edificio. Efecto secundario correcto: `AccessCodeExpirationCron` dejó de unirse contra `Appointment` para su filtro por inquilino, y de paso empezó a barrer también los códigos ad-hoc vencidos sin ningún caso especial) | backend |
| 4.9 Endurecimiento, cumplimiento y piloto | en progreso | FASE-8_PROMPT-5 (TASK-69, P8.4: empaquetado y despliegue con Docker — imagen de dos etapas (compila con las dependencias de desarrollo, incluidos el compilador de TypeScript y el CLI de Prisma, luego poda esas dependencias sin perder el cliente de Prisma ya generado, que vive dentro de una dependencia de producción) corriendo con usuario sin privilegios en la etapa final; `docker-compose.prod.yml` distinto del ya existente para desarrollo local (que solo levanta la base de datos, para no forzar una reconstrucción de imagen en cada guardado), con tres servicios — base de datos, migraciones de Prisma en un contenedor de una sola ejecución antes de que el backend arranque, y el backend, único que expone puerto al host — sobre red interna y volumen persistente; ninguna variable de entorno obligatoria tiene valor de respaldo en ese archivo, el stack se niega a arrancar nombrando la que falte. Validar el despliegue de punta a punta contra Docker real (no solo revisar los archivos) expuso un defecto preexistente: la raíz de compilación de TypeScript nunca estaba fijada, así que el compilador la calculaba como el directorio del proyecto entero al encontrar el script de seed fuera de `src/` (ejecutado aparte, con otra herramienta), y el archivo de entrada terminaba un nivel más adentro de lo que el comando de arranque de producción ya existente esperaba — nadie lo había notado porque ese comando nunca se había corrido de punta a punta antes; corregido fijando esa raíz en `tsconfig.build.json` y excluyendo el script de esa compilación, sin afectarlo. Guía de despliegue paso a paso (`docs/deployment.md`) para un tercero sin conocimiento previo del código: prerrequisitos, variables de entorno, nota de que el Gateway WiFi G2 de TTLock es un dispositivo físico ajeno a este stack de contenedores (el backend solo habla con la nube de TTLock), y estrategia de backup documentada —no implementada, por el alcance de la tarea y porque un trabajo de archivado automático todavía no existe (tarea de retención anterior)— con copia en frío del volumen y volcado lógico periódico en caliente. Credenciales de TTLock deliberadamente ausentes de las variables de entorno, ni siquiera como ejemplo, por ser per-tenant y leerse de la base de datos desde una decisión de arquitectura de tareas anteriores. Verificado levantando el stack completo desde cero (migraciones corren y terminan antes de que el backend arranque, endpoint de salud responde), y confirmando persistencia de datos tras `down`/`up -d` sin destruir el volumen; suite unitaria (75 suites/753 pruebas) y linter en verde tras la corrección de `tsconfig.build.json`); FASE-8_PROMPT-4 (TASK-68, P8.3: suite de tests integral y cobertura — cierra el módulo de tests consolidando lo que cada tarea de extremo a extremo de los módulos anteriores ya había construido por separado. Cobertura medida solo con la suite unitaria: 76% líneas/67% ramas, por debajo del umbral, no por falta de pruebas sino porque buena parte de la capa de servicios (pacientes, profesionales, matrículas, ausencias) se valida solo por extremo a extremo; se unificó la configuración de Jest (antes repartida entre `package.json` y un archivo aparte) en dos proyectos de una sola configuración y se midió el umbral ejecutándolos juntos — Jest combina la cobertura de todos los proyectos de una misma invocación de forma nativa, sin herramienta externa de fusión —, resultando 97% líneas/83% ramas, muy por encima del 80% exigido, sin escribir ninguna prueba nueva solo para el número. El umbral se acota a `*.service.ts` y excluye a los adaptadores de integración externa (`*.adapter.ts`) por convención de nombres ya existente, sin lista de exclusión a mano. Se descubrió que la clave de Jest para acotar el umbral a un subconjunto de archivos no promedia el conjunto sino que exige que cada archivo lo supere individualmente (contrario a lo que sugiere la documentación de la herramienta) — se resolvió sin esa clave, restringiendo de antemano qué archivos se miden y usando el umbral general como el promedio agregado buscado. Verificó (sin pruebas nuevas) que el aislamiento multi-tenant y las pruebas de seguridad básicas (401/403/límite de tasa en login) ya existían en capas distintas y complementarias, documentando en `CLAUDE.md` cuál prueba cubre cada caso. Integración continua ampliada con un paso que ejecuta la suite combinada con cobertura tras las corridas ya existentes — la suite de extremo a extremo corre dos veces por ejecución, decisión deliberada para mantener una señal de fallo rápida separada de la corrida instrumentada); FASE-8_PROMPT-3 (TASK-67, P8.2: retención de datos y derechos del titular, Ley 25.326/26.529 — política de retención documentada con `COMMENT ON` de PostgreSQL vía migración escrita a mano (Prisma no declara comentarios de tabla/columna en su esquema), plazos de cinco años propios del sistema al no fijar la SRS ni el anteproyecto un número; verificación, sin cambios de código, de que el registro de auditoría nunca guarda contenido del paciente en sus veintiún puntos de escritura; tres endpoints nuevos bajo `/admin/pacientes/:id` en un módulo propio (evita un ciclo de importación con el módulo de turnos) — exportación completa en JSON/CSV excluyendo las observaciones internas del profesional por la misma restricción de una tarea anterior, supresión que anonimiza los campos identificatorios (nunca borrado físico, una FK ya existente lo impide) rechazando con 409 y nombrando los turnos si hay alguno reservado/confirmado a futuro, verificación bajo aislamiento serializable para evitar una reserva huérfana entre el chequeo y la escritura, y rectificación que reutiliza el método de edición existente con un nombre de acción de auditoría propio; reverificación de que el consentimiento sigue siendo condición necesaria para reservar turno, ya implementado); FASE-8_PROMPT-2 (TASK-66, P8.1: seguridad — revisión de guards de autorización en todos los controladores, sin vacíos encontrados (la cobertura y el aislamiento entre organizaciones, por extensión de Prisma acotada por inquilino, ya eran completos de tareas anteriores); límite de tasa (`@nestjs/throttler`) sólo en `POST /auth/login` y `POST /webhook`, dos perfiles nombrados activados por ruta y no como guard global, configurables por variable de entorno con los valores del SRS por defecto — elevados sólo en `.env`/integración continua porque la suite de extremo a extremo inicia sesión más de diez veces por archivo, con una prueba aparte que restablece el valor real sobre una instancia aislada; escaneo de secretos con `gitleaks` agregado a la integración continua sobre el historial completo; cifrado en reposo con AES-256-GCM de `AccessCode.ttlockPasscodeId` —el identificador reutilizable de la cerradura, no el PIN, ya excluido de todo registro desde una tarea anterior—, resuelto y aplicado en los dos únicos puntos que tocan la columna, sin extensión genérica de Prisma por el costo de adaptar las pruebas de extremo a extremo existentes que construyen filas directamente; decisión explícita, no pendiente, de no cifrar a nivel de columna la fecha de nacimiento/celular de paciente ni las observaciones de la relación paciente-profesional, apoyada en cifrado de disco); FASE-8_PROMPT-1 (sin ticket: auditoría de integridad/normalización/relaciones del esquema real contra `psique_dev`, previa a los módulos siguientes. Confirmó que las 38 FK y los 5 `CHECK` documentados existen en la base real. Corrigió una FK compuesta incompleta en `WaitlistOffer.waitlistEntry` — de dos a tres columnas, atando también el paciente —, reconcilió con `prisma migrate resolve --rolled-back` un ledger de migraciones inconsistente que bloqueaba `migrate dev` desde TASK-53, y limpió filas de prueba acumuladas en el catálogo global `HealthInsurer`) | backend |
| 4.10 Presentación de la solución | sin iniciar | — | — |

## Capítulo 5: Resultados, Conclusiones y Trabajo Futuro

| Subsección | Estado | Último prompt | Componente |
|---|---|---|---|
| Resultados | sin iniciar | — | — |
| Conclusiones | sin iniciar | — | — |
| Trabajo futuro | sin iniciar | — | — |

## Notas

- "FASE-0_PROMPT-EJEMPLO" (backend) es la entrada de muestra creada al
  configurar la skill `documentacion-tesis`, no una tarea real numerada;
  sirve solo para validar el formato. Ver
  `tesis/bitacora/FASE-0_PROMPT-EJEMPLO_BACKEND.md`.
- El Capítulo 2 completo (2.1 a 2.8) está en `tesis/capitulos/cap2_marco_teorico_a.md`
  (2.1–2.4) y `cap2_marco_teorico_b.md` (2.5–2.8). Se marca `borrador` y no
  `completo` porque el texto contiene marcadores `[CITA: ...]` sin fuente
  verificada (ver `tesis/referencias_pendientes.md`) y marcadores
  `[VERIFICAR: ...]` sobre datos volátiles (versiones, precios, límites de
  API) todavía no confirmados. Pasa a `completo (pendiente de revisión)`
  recién cuando ambos tipos de marcador se resuelvan.
- Estructura de 5 capítulos vigente desde 2026-08-26 (antes: Cap. 3
  "Solución PSIQUE" mezclaba análisis, diseño y desarrollo). La app móvil
  entra solo como diseño de prototipo en 3.7; su desarrollo es trabajo
  futuro (Capítulo 5), por lo que no hay subsección móvil en el Capítulo 4.
- Los huecos en la numeración (1.7 y 4.8) son deliberados, reservados para
  contenido a definir; no son errores de transcripción.
- El desarrollo del backend se documenta en
  `tesis/capitulos/cap4_desarrollo.md` (renombrado desde el archivo de
  Desarrollo anterior a la reestructuración de 5 capítulos); la
  arquitectura general (3.4) y el diseño del prototipo móvil (3.7) se
  documentan en `tesis/capitulos/cap3_analisis_diseno.md`.
